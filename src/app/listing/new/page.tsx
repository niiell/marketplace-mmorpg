import React from 'react';
import NewListingWrapper from "./NewListingWrapper";

/**
 * Page component that renders the NewListingWrapper.
 *
 * @returns {JSX.Element} The JSX element representing the page.
 */
export default function Page() {
  return (
    <React.Fragment>
      <NewListingWrapper />
    </React.Fragment>
  );
}